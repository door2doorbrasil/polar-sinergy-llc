name: Deploy to VPS

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

concurrency:
  group: deploy-vps
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy through Hostinger API
        env:
          HOSTINGER_API_TOKEN: ${{ secrets.HOSTINGER_API_TOKEN }}
          HOSTINGER_VPS_ID: ${{ secrets.HOSTINGER_VPS_ID }}
          HOSTINGER_REPO_URL: https://github.com/${{ github.repository }}
          HOSTINGER_PROJECT_NAME: ${{ secrets.HOSTINGER_PROJECT_NAME }}
          HOSTINGER_SMOKE_URL: ${{ secrets.HOSTINGER_SMOKE_URL }}
          HOSTINGER_PURGE_ALL: "1"
        run: |
          python3 deploy/hostinger/deploy.py
