name: polar-sinergy

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: polar-sinergy-web:local
    container_name: polar-sinergy-web
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-18080}:80"
